[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    eval: PHI-recall evaluation runs (the deterministic-backend run is part of the default suite)
    slow: slow tests (optional NER backends, large corpora); deselect with -m "not slow"
